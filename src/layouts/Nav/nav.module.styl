// nav
$navbar-top-height = 50px;
$navbar-bottom-height = 30px;
$nav-link-padding = 10px 15px;
$navbar-padding-vertical = floor(($grid-gutter-width / 2));

$navbar-pad-horiz($height)
  (($height - $line-height-computed) / 2)


$nav-padding($height)
  padding $navbar-pad-horiz($height) $navbar-padding-vertical

$navbar($height, $font-size = 20px)
  height $height
  font-size $font-size
  width 100%
  li
    display inline-block
    height: 100%
    text-align center
    > a
      height: 100%
      cursor pointer
      display block
      $nav-padding($height)

// required to make dropdown menu work
.navbar
  position relative
  z-index $z-navbar

.top
  $grid()
  $navbar($navbar-top-height)
  padding-left $navbar-padding-vertical
  padding-right $navbar-padding-vertical
  border-bottom 1px solid $gray
  display flex
  justify-content space-between


.hamburger
  flex 1
  align-self center
  @media (min-width: 760px)
    visibility hidden
    //todo: fix media query

.title
  $nav-padding($navbar-top-height)
  flex 3
  font-size 24px
  font-weight bold
  text-align center

.account
  flex 1
  text-align right

.cart
  img
    size($navbar-top-height - ($navbar-pad-horiz($navbar-top-height) *2))

.bottom
  $navbar($navbar-bottom-height, 14px)
  $row(center)
  border-bottom 1px solid $gray
  ul
    display inline-block
    a
      $column(center, center)
      text-transform uppercase
      letter-spacing 2.75px
      color $menu-unselected
      font-weight 100
      margin-left 17px
      margin-right 17px
      padding-left 0
      padding-right 0
      &:hover
        color $menu-selected
      &:active
        color $menu-selected
        padding-top 5px
        border-bottom 5px solid black
